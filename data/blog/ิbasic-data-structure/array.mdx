---
title: โครงสร้างข้อมูล Array
date: '2021-08-09'
tags: ['data structure', 'c']
draft: true
summary: ทำความรู้จักกับ โครงสร้างข้อมูลที่มีชื่อว่า Array และ ยกตัวอย่างวิธีการนำ Array
---

## Introduction

อาร์เรย์(Array) เป็นโครงสร้างข้อมูลที่นำเซลล์ข้อมูล (Data cell) มาเรียงต่อกันเป็นกลุ่มข้อมูล โดยข้อมูลแต่ละเซลล์ในอาร์เรย์จะมีชนิดข้อมูลพื้นฐาน(Integer, float, double, ...) เป็นชนิดข้อมูลเดียวกันทั้งกลุ่ม และ ต้องกำหนดขนาดของอาร์เรย์หรือจำนวนเซลล์ข้อมูลที่จะจัดเก็บก่อนที่จะนำไปใช้งาน

การเข้าถึงตำแหน่งเซลล์ข้อมูลในอาร์เรย์จะใช้ชื่ออาร์เรย์และตำแหน่งเซลล์ในการอ้างอิงตำแหน่งข้อมูล(Index) ในอาร์เรย์ โดยมีรูปแบบและตัวอย่างใครงสร้างอาร์เรย์ในการเก็บข้อมูล

```
arrayName[Index]
```
