---
title: Data Structure and Algorithm 1O1!
date: '2021-08-07'
tags: ['data structure', 'algorithm', 'c']
draft: false
summary: ทำความรู้จักกับ โครงสร้างข้อมูล และ อัลกอริทึม พร้อมยกตัวอย่างวิธีการนำ Array และ Bubble Sort ไปใช้งานในภาษา C
---

## Introduction

สำหรับคนที่พึ่งเริ่มหัดเขียนโปรแกรมในช่วงแรก นอกจากการที่เราจะต้องเรียนรู้ [Syntax](<https://en.wikipedia.org/wiki/Syntax_(programming_languages)>) ของภาษาโปรแกรมมิ่งแล้วนั้น ยังมีอีกเรื่องที่สำคัญไม่แพ้กัน นั้นคือการทำความเข้าใจในเรื่อง [Data Structure](https://en.wikipedia.org/wiki/Data_structure) และ [Algorithm](https://en.wikipedia.org/wiki/Algorithm)

เพื่อที่เราจะสามารถนำไปประยุกต์ใช้ในการแก้ปัญหาต่างๆ รวนแต่ต้องใช้งานโครงสร้างข้อมูล และ
อัลกอริทึมที่เหมาะสม เพื่อที่จะทำให้แก้ไขปัญหาได้อย่างมีประสิทธิภาพมากที่สุด และ รวดเร็วที่สุด

> จะเรียนรู้โครงสร้างข้อมูลกับอัลกอริทึมให้ปวดหัวไปทำไม?

![Image Meme](https://qph.fs.quoracdn.net/main-qimg-6d3f7f40462eaff54212173cca1874f7.webp)

---

## โครงสร้างข้อมูล และ อัลกอริทึม คืออะไร

**โครงสร้างข้อมูล (Data Structure)** คือ การจัดการข้อมลในหน่วยความจำภายในเครื่องคอมพิวเตอร์ หรือ บางครั้งเป็นการจัดการข้อมูลในดิสก์ให้มีความสัมพันธ์กันภายในกลุ่มข้อมูล ให้มีรูปแบบและข้อกำหนดที่ชัดเจนในการกำหนดคุณสมบัติเพื่อสร้างความสัมพันธ์ภายในกลุ่มข้อมูล

> รูปแบบการเก็บข้อมูลที่อยู่ในโครงสร้างข้อมูล เช่น อาร์เรย์(Array), ลิงค์ลิสต์(Linked-List), สแต็ก(Stack), ไบนารีทรี(Binary Tree) เป็นต้น

**อัลกอริทึม (Algorithm)** หรือเรียกอีกอย่างว่า **ขั้นตอนวิธี** เป็นวิธีการแสดงลำดับขั้นตอนในการทำงาน หรือ การลำดับขั้นตอนในการแก้ไขปัญหา เช่น การกำหนดขั้นตอนเพื่อแก้ไขปัญหาการจัดเรียงเอกสารในแฟ้มข้อมูล หรือ การกำหนดอัลกอรึทึมในการค้นหาข้อมูลในแฟ้มข้อมูลทั้งหมด เป็นต้น

> อัลกอริทึมหรือขั้นตอนวิธีพบได้ในชีวิติประจำวัน เช่น ขั้นตอนการเติมเงินโทรศัพท์มือถือผ่านตู้เติมเงินอัตโนมัติ, ขั้นตอนการใช้งานตู้กดเงินอัตโนมัติ(ATM) เพื่อทำธุรกรรมทางการเงิน เป็นต้น

สำหรับคนที่ต้องการศึกษาอัลกอริทึมในแต่ละประเภท สามารถเข้าไปอ่านได้ที่นี่: [10 ประเภทของอัลกอริทึม](https://blog.1tpp.com/blog/types-of-algorithms)

## ตัวอย่างใช้งานโครงสร้างข้อมูล และ อัลกอริทึม

```c:algorithm1O1_example.c showLineNumbers
#include <stdio.h>

// Sorting algorithm
int* bubbleSort(int theArray[], int n) {
  for (int pass = 1; pass < n; pass++) {
    for (int index=0; index < n-pass; index++) {
      int nextIndex = index+1;
      if(theArray[index] > theArray[nextIndex]) {
        int temp = theArray[index];
        theArray[index] = theArray[nextIndex];
         theArray[nextIndex] = temp;
      }
    }
  }
  return theArray;
}


void main() {
  int numbers[] = {2, 1, 3, 0, -2, -128, 99};
  int sizeOfArray = sizeof(numbers) / sizeof(int);

  printf("Array before sorting: ");
  for (int index = 0; index < sizeOfArray; index++) {
    printf("%d, ", numbers[index]);
  }
  printf("\n");


  int* sortedArray = bubbleSort(numbers, sizeOfArray);
  printf("Array after sorting: ");
  for (int index = 0; index < sizeOfArray; index++) {
    printf("%d, ", numbers[index]);
  }
}
```

```
Array before sorting: 2, 1, 3, 0, -2, -128, 99,
Array after sorting: -128, -2, 0, 1, 2, 3, 99,
```

ในตัวอย่างเรามีการใช้งานโครงสร้างข้อมูล **Array** ที่มีชนิดเป็น `Integer` โดยเก็บข้อมูลชุดตัวเลขที่ยังไม่ได้มีการเรียงลำดับ
และเราได้สร้างฟังก์ชันที่ใช้ในการจัดเรียงข้อมูลที่มีชื่อว่า **bubbleSort** โดยรับค่า `theArray` และ `n`
โดยการทำงานของฟังก์ชั่นจะทำการเรียงลำดับจากตัวเลขที่มีค่าต่ำสุดไปยังตัวเลขที่มีค่าสูงสุดโดยใช้ Sorting algorithm ที่มีชื่อว่า Bubble Sort และมีการ Return ค่า **Array** ที่เป็นข้อมูลที่ถูกเรียงลำดับตัวเลข

## Conclusion

ในวันนี้เราได้มาทำรู้จักกับโครงสร้างฐานข้อมูลและอัลกอริทึมกันแล้วว่ามันคืออะไร และได้ลองใช้งานโครงสร้างข้อมูล Array ใช้งาน Sorting Algorithm ที่มีชื่อว่า Bubble Sort ในภาษา C และ หวังว่าบทความนี้จะมีประโยชน์สำหรับทุกคนนะครับ 🙃
